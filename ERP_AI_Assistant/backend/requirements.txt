# Claude API
anthropic==0.8.1

# Embeddings only (for RAG)
sentence-transformers==2.2.2

# Vector database
chromadb==0.4.18

# Web framework
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0

# Utilities
python-dotenv==1.0.0

# Note: All fallback AI models removed - using only Claude API + RAG
# Removed: transformers, torch, numpy, pandas, spacy, nltk, faiss-cpu
# Built-in modules (no installation needed): sqlite3, logging, json, re, datetime
